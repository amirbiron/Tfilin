# קובץ דוגמה למשתני סביבה
# העתק קובץ זה ל-.env ומלא את הערכים

# טוקן הבוט מטלגרם (קבל מ-@BotFather)
BOT_TOKEN=1234567890:ABCDEFghijklmnopQRSTUVwxyz1234567890

# כתובת MongoDB (לרב המקרים MongoDB Atlas)
# דוגמה ל-MongoDB Atlas:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tefillin_bot?retryWrites=true&w=majority

# או MongoDB מקומי לפיתוח:
# MONGODB_URI=mongodb://localhost:27017/tefillin_bot

# הגדרות אופציונליות (אם לא מוגדרות יילקחו ברירות מחדל)

# שעת ברירת מחדל לתזכורת
DEFAULT_REMINDER_TIME=07:30

# אזור זמן (ברירת מחדל: Asia/Jerusalem)
DEFAULT_TIMEZONE=Asia/Jerusalem

# רמת לוגים (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# האם לדלג על שבת וחגים (true/false)
SKIP_SHABBAT=true
SKIP_HOLIDAYS=true

# הגדרות פיתוח
# NODE_ENV=production
# DEBUG=false

# הערות חשובות:
# 1. אל תשתף את הקובץ .env עם אף אחד!
# 2. הוסף .env לקובץ .gitignore
# 3. ב-Render הגדר את המשתנים במסך Environment Variables
# 4. הטוקן BOT_TOKEN חובה - ללא זה הבוט לא יעבוד
# 5. MONGODB_URI חובה - ללא זה הבוט לא יצליח להתחבר למסד הנתונים

# הוראות הקמה:
# 1. צור בוט חדש אצל @BotFather בטלגרם
# 2. קבל את הטוקן והכנס אותו כ-BOT_TOKEN
# 3. הכן מסד נתונים MongoDB (מומלץ MongoDB Atlas - חינמי)
# 4. העתק את כתובת החיבור ל-MONGODB_URI
# 5. שמור את הקובץ כ-.env (ללא .example)
# 6. הרץ את הבוט!
